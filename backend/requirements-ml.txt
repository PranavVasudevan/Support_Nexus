# ──────────────────────────────────────────────────────────────────────────
#  ML / TRAINING dependencies — only needed to TRAIN DistilBERT or to run the
#  on-prem DistilBERT inference path. Install on top of requirements.txt:
#
#      pip install -r requirements.txt -r requirements-ml.txt
#
#  GPU NOTE (NVIDIA, e.g. RTX 3050):
#  Install the CUDA build of torch FIRST so you get GPU acceleration, then
#  install the rest of this file:
#
#      pip install torch --index-url https://download.pytorch.org/whl/cu124
#      pip install -r requirements-ml.txt
#
#  Without the CUDA build, torch falls back to CPU (training is much slower
#  but still works).
# ──────────────────────────────────────────────────────────────────────────

torch>=2.4.0               # use the cu124 build for GPU (see note above)
# Cap transformers: 4.50+ requires torch>=2.7 (uses torch.float8_e8m0fnu).
# 4.46.x works with the cu124 torch 2.6 build installed above.
transformers>=4.44.0,<4.49
datasets>=2.20.0
accelerate>=0.33.0
scikit-learn>=1.4.2
numpy>=1.26.4
pandas>=2.2.2

# Experiment tracking (optional — only used if MLFLOW_URI is set)
mlflow>=2.11.0
